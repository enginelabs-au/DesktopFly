# backend

Python 3.12 flysim package (config, adapters, ingest, LIF, supervisor). Not implemented in phase 0.

`real_graph_enabled` stays false while Q-012 is a release requirement. Do not start a LIF worker from this tree until that decision changes.
